export interface UserProfile {
    id?: number,
    username?: string,
    email?: string,
    name?: string,
    phone?: string,
    website?: string,
    avatar: string,
    address?: string,
    company?: string,
    dob?: Date
}